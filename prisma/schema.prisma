generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model checkmate {
  game String @id @db.VarChar
}

model map {
  start Int     @id
  game  String? @db.VarChar
}
